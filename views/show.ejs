<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Blog App - <%= blog.title %></title>
</head>
<body>
  <h1><%= blog.title %></h1>
  <p><%= blog.body %></p>
  <img src="<%= blog.image %>" alt="<%= blog.title %>">
  <a href="/">Go Back</a>
</body>
</html>

